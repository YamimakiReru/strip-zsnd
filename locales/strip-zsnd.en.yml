en:
  app.description: |
    Strip consecutive zero samples in WAV files

    Removes consecutive zeros caused by buffer underflow during recording.
    Waveforms that have cliffs in the middle cannot be repaired.

  zsnd.args.detect: Detects zero-runs without creating any output file.
  zsnd.args.min_duration: 'Minimum duration considered a dropout. Unit: milliseconds.'
  zsnd.args.threshold: 'Volume threshold considered zero. Unit: dB.'

  app.args.input: Path to the input file.
  app.args.output: 'Path to the output file. default: [input]-fixed'
  app.args.verbose: increases logging verbosity (e.g., -v, -vv, -vvv).
  app.processing: Procesamiento...

#         'r.error.unsupported_format': 'Unsupported format'
  r.error.unsupported_format_exc: 'Unsupported format: %%s'
  r.error.mono_only_supported: Supports mono audio sources only

  zsnd.input_file_cannot_be_opened: |
    Input file "%%(f)s" cannot be opened:
    %%(exc)s
  zsnd.output_file_cannot_be_opened: |
    Output file "%%(f)s" cannot be opened:
    %%(exc)s
#         'cwd.zero_run_detected': '%(abs_start)s-%(abs_end)s (%(length)d samples) lacked',

  click.subcommand_metavar: '[ARGS]...'

  # use default messages
  click.options_metavar:
